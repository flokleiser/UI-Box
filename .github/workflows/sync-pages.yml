name: Sync src/pages from dev to merge

permissions:
  contents: write

on:
  push:
    branches:
      - dev

jobs:
  sync-pages:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Checkout merge branch
        run: git checkout merging-test

      - name: Sync pages, components and styles
        run: |
          git checkout dev -- src/pages src/styles.css
          git add src/pages src/styles.css
          git commit -m "Sync src/pages and src/styles.css from dev branch" || echo "No changes to commit"

      - name: Push changes to merge
        run: git push merging-test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
